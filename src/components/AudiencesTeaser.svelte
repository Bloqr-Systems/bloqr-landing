<section class="audiences-teaser" id="personas" aria-labelledby="audiences-teaser-heading">
  <div class="container">
    <div class="section-header">
      <p class="section-label">Who Bloqr is for</p>
      <h2 class="section-title" id="audiences-teaser-heading">Pick your starting point.</h2>
      <p class="section-desc">Choose the workflow that matches how you browse, build, or support others.</p>
    </div>

    <div class="persona-pills" aria-label="Audience personas">
      <span class="persona-pill">Consumers</span>
      <span class="persona-pill">Developers</span>
      <span class="persona-pill">Families</span>
      <span class="persona-pill">Partners</span>
    </div>

    <a href="#audience-wizard" class="teaser-link">Find your fit →</a>
  </div>
</section>

<style>
  .audiences-teaser {
    padding: 72px 0;
    border-top: 1px solid var(--border);
  }

  .section-desc {
    max-width: 640px;
  }

  .persona-pills {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
    margin-bottom: 20px;
  }

  .persona-pill {
    padding: 10px 14px;
    border-radius: 999px;
    border: 1px solid var(--border-2);
    background: var(--bg-surface);
    color: var(--text-1);
    font-size: 0.85rem;
    font-weight: 600;
    letter-spacing: 0.02em;
  }

  .teaser-link {
    display: inline-flex;
    align-items: center;
    color: var(--cyan);
    text-decoration: none;
    font-weight: 600;
    font-size: 0.95rem;
  }

  .teaser-link:hover {
    text-decoration: underline;
  }
</style>
