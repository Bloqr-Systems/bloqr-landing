---
import { PLAUSIBLE_HOST } from '../config';
const isProd = import.meta.env.PROD;
const plausibleDomain = import.meta.env.PUBLIC_PLAUSIBLE_DOMAIN ?? 'bloqr.dev';
---
{isProd && (
  <script defer data-domain={plausibleDomain} src={`${PLAUSIBLE_HOST}/js/script.js`} is:inline></script>
)}
